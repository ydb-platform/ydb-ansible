# Active options for run-* scripts

# Useful extra options:
#  -k  for password prompt
RUN_ANSIBLE="ansible-playbook -b -f 20 -i hosts"
RUN_ANSIBLE_PARALLEL="ansible-playbook -b -i hosts"

# End Of File